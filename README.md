# TypeScript stub

_A sample stub project for typescript._

## Development

Run the tests:

```sh
npm install
npm test
```

Compile the typescript to javascript:

```sh
npm run build
```

Run `index.ts`:

```sh
npm start
```
